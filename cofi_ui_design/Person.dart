import 'package:flutter/material.dart';

class person extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Music',style: TextStyle(fontWeight: FontWeight.bold,fontSize: 40,color: Colors.red),)),
    );
  }
}
