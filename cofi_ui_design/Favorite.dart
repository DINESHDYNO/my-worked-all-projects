import 'package:flutter/material.dart';

class favorite extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Search',style: TextStyle(fontWeight: FontWeight.bold,fontSize: 40,color: Colors.red),)),
    );
  }
}
